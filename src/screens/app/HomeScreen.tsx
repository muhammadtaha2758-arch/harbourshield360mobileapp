/**
 * Customer home / dashboard — same screen as the Dashboard tab (home.vue).
 * The tab navigator uses `DashboardScreen`; this export keeps the HomeScreen path aligned.
 */
export { DashboardScreen as HomeScreen } from './DashboardScreen';
